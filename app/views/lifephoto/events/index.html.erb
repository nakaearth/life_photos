<h1>Listing events</h1>
<div class="content" ng-controller="EventsCtrl">
	<div class="content_div">
		<u1 ng-show="events.length" style="list-style:none;padding:0">
		件数：{{events.length}}
    	<li ng-repeat="event in events" ng-class-even="'evenbg'" ng-class-odd="'oddbg'">
    	{{event.title}} {{event.start}} から {{event.end}} |&nbsp;&nbsp;<a href='/events/{{event.id}}' >詳細</a> &nbsp;&nbsp;
    	</li>
    </u1>
  </div>
<br>

<%= link_to 'New Event', new_lifephoto_event_path %>
</div>
